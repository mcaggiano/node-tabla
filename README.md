# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es el número base de la tabla a crear      [número] [requerido]
  -l, --listar   Muestra la tabla creada en consola  [booleano] [defecto: false]
  -h, --hasta    Se multiplicará hasta el número indicado aquí
                                                          [número] [defecto: 1]
```
